# Supabase Configuration (Required)
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_KEY=your_supabase_service_key

# JWT Configuration (Optional - Supabase handles this)
JWT_SECRET=your_jwt_secret_key

# Server Configuration (For local development)
PORT=3001  # API server port
API_PORT=3001  # Same as PORT for API
NODE_ENV=development

# Google OAuth (Optional - configured in Supabase dashboard)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Frontend URL Configuration (Optional)
# The system automatically detects the frontend URL:
# - Production (Vercel): Uses deployment URL from request headers
# - Development: Uses localhost:3000
# You can override by setting FRONTEND_URL if needed:
# FRONTEND_URL=https://your-custom-domain.com

# Note: OAuth Redirects
# Google OAuth and email verification will automatically use the correct URL
# based on where the request comes from (production or development)