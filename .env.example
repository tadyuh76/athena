# Supabase Configuration (Required)
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_KEY=your_supabase_service_key

# JWT Configuration (Optional - Supabase handles this)
JWT_SECRET=your_jwt_secret_key

# Server Configuration (For local development)
PORT=3001  # API server port
API_PORT=3001  # Same as PORT for API
NODE_ENV=development

# Google OAuth (Optional - configured in Supabase dashboard)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Note: Frontend URL Configuration
# The frontend automatically detects the environment:
# - Production (Vercel): Uses deployment URL (e.g., https://ueh-athena.vercel.app)
# - Development: Uses localhost:3000
# No need to manually set BASE_URL or SITE_URL